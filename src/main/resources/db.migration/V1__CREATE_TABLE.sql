CREATE TABLE IF NOT EXISTS tabela_teste_inicial (
                                                    id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                                    nome VARCHAR(255)
    );
